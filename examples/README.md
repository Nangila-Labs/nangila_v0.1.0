# Examples Layout

The `examples/` tree contains non-stable example code.

- `experimental/`: prototypes, validation scripts, and exploratory examples that are not part of the `v0.1` support contract

The active `v0.1` user path is documented in [`README.md`](/Users/craigchirara/nangila/README.md), not through the experimental examples.
